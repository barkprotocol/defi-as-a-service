/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'uploadcare.com',
      'ucarecdn.com',
      'anotherdomain.com',
    ],
  },
};

export default nextConfig;
