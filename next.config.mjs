/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'uploadcare.com',
        'anotherdomain.com',
      ],
    },
  };
  
  export default nextConfig;
  